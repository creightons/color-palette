let initialPaletteState = {
  colors: ['#fff'],
  currentIndex: 0,
};

let mainPalette = (
  state = initialPaletteState,
  action
) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default mainPalette;